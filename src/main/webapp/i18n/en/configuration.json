{
  "configuration": {
    "title": "Configuration",
    "resetCache": "Reset Cache",
    "filter": "Filter (by prefix)",
    "table": {
      "prefix": "Prefix",
      "properties": "Properties"
    }
  }
}
